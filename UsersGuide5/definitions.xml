<?xml version="1.0"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                 "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">

  <chapter id="definitions">
    <title>Definitions and Terms</title>

    <para>
      This chapter defines common terms that pertain to the Firewall
      Builder program.
    </para>

    <variablelist>
      <varlistentry>
	<term>
	Firewall
	</term>
      <listitem>
	<para>The term <emphasis>firewall</emphasis> can refer to a
	device that implements firewall software, or it can refer to
	the complete set of software and policies running on the device, or it can refer
	to just a firewall access policy.</para>

	<para>In this document, the term <emphasis>firewall</emphasis>
	refers the firewall object in Firewall Builder. A
	firewall <emphasis>object</emphasis> contains a logical representation of a
	firewall device's interfaces, its access policy rule set, its
	NAT rule set, and the routing rule set to be placed on the
	firewall device.</para>
	</listitem>
      </varlistentry>
      <varlistentry>
	<term>
	Policy
	</term>
      <listitem>
	<para>The term <emphasis>policy</emphasis> can refer to the
	entire set of "business logic" that is implemented in the
	firewall or it can refer to the access policy portion of
	firewall only.</para>

	<para>In this document, the term <emphasis>policy</emphasis>
	refers to the access policy rule set.</para>
	</listitem>
      </varlistentry>
    </variablelist>
  </chapter>
